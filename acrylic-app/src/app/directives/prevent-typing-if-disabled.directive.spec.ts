import { PreventTypingIfDisabledDirective } from './prevent-typing-if-disabled.directive';

describe('PreventTypingIfDisabledDirective', () => {
  it('should create an instance', () => {
    const directive = new PreventTypingIfDisabledDirective();
    expect(directive).toBeTruthy();
  });
});
