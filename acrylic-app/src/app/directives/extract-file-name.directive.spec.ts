import { ExtractFileNameDirective } from './extract-file-name.directive';

describe('ExtractFileNameDirective', () => {
  it('should create an instance', () => {
    const directive = new ExtractFileNameDirective();
    expect(directive).toBeTruthy();
  });
});
